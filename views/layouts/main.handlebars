<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CMS</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css">
</head>

<body class="bg-gray-100 h-screen">
  <div class="container mx-auto p-6">
    <header class="flex justify-between items-center">
      <h1 class="text-3xl">
        <a href="/" class="text-blue-500">TechBlog</a>
      </h1>
      <nav>
        <a href="/" class="text-gray-700">Homepage</a> |
        {{#if logged_in}}
        <a href="/dashboard" class="text-gray-700">dashboard</a> | <button id="logout" class="bg-red-500 text-white p-2">logout</button>
        {{else}}
        <a href="/login" class="text-gray-700">login</a>
        {{/if}}
      </nav>
    </header>
    <main class="my-6">
      {{{ body }}}
    </main>
    <footer class="text-center text-gray-700">

    </footer>
  </div>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
